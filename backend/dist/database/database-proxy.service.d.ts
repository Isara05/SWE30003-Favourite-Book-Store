import { Book, Customer, Staff, Invoice, Order, Payment, TradeInRecord, UserAccount, InstallmentPlan, InventoryAuditRecord } from '../domain/models';
export declare class DatabaseProxyService {
    private static instance;
    private readonly booksFile;
    private readonly usersFile;
    private readonly ordersFile;
    private readonly invoicesFile;
    private readonly tradeInsFile;
    private readonly paymentsFile;
    private readonly installmentPlansFile;
    private readonly inventoryAuditsFile;
    constructor();
    private keyLocks;
    private acquireLock;
    withLocks<T>(keys: string[], callback: () => Promise<T>): Promise<T>;
    static getInstance(): DatabaseProxyService;
    private toCustomer;
    private toStaff;
    private fromCustomer;
    private fromStaff;
    getUsers(): Promise<UserAccount[]>;
    saveUsers(users: UserAccount[]): Promise<void>;
    getBooks(): Promise<Book[]>;
    getAllBooks(limit?: number, offset?: number): Promise<Book[]>;
    getBookByISBN(isbn: string): Promise<Book | undefined>;
    insertBook(book: Book): Promise<Book>;
    updateBookDetails(isbn: string, updatedBook: Book): Promise<Book | null>;
    deleteBook(isbn: string): Promise<boolean>;
    getBooksPage(limit?: number, offset?: number): Promise<Book[]>;
    executeReader(limit?: number, offset?: number): Promise<Book[]>;
    saveBooks(books: Book[]): Promise<void>;
    checkBookExists(isbn: string): Promise<boolean>;
    updateStockLevel(isbn: string, quantity: number): Promise<Book | null>;
    getCustomers(): Promise<Customer[]>;
    getCustomerByID(customerId: string): Promise<Customer | undefined>;
    getCustomersWithOutstandingBalances(): Promise<Array<Customer & {
        outstandingBalance: number;
    }>>;
    saveCustomers(customers: Customer[]): Promise<void>;
    insertCustomer(customer: Customer): Promise<Customer>;
    updateCustomerBalance(customerId: string, newBalance: number): Promise<Customer | null>;
    updateCustomerDetails(customerId: string, details: Partial<Customer>): Promise<Customer | null>;
    deleteCustomer(customerId: string): Promise<boolean>;
    getStaff(): Promise<Staff[]>;
    saveStaff(staff: Staff[]): Promise<void>;
    getOrders(): Promise<Order[]>;
    saveOrders(orders: Order[]): Promise<void>;
    getInvoices(): Promise<Invoice[]>;
    saveInvoices(invoices: Invoice[]): Promise<void>;
    insertInvoice(invoice: Invoice, orderId: string): Promise<Invoice>;
    saveInvoice(invoice: Invoice): Promise<Invoice>;
    getTradeIns(): Promise<TradeInRecord[]>;
    saveTradeIns(records: TradeInRecord[]): Promise<void>;
    getInstallmentPlans(): Promise<InstallmentPlan[]>;
    saveInstallmentPlans(plans: InstallmentPlan[]): Promise<void>;
    insertInstallmentPlan(plan: InstallmentPlan): Promise<InstallmentPlan>;
    getInventoryAudits(): Promise<InventoryAuditRecord[]>;
    saveInventoryAudits(records: InventoryAuditRecord[]): Promise<void>;
    insertInventoryAudit(record: InventoryAuditRecord): Promise<InventoryAuditRecord>;
    getPayments(): Promise<Payment[]>;
    savePayments(payments: Payment[]): Promise<void>;
    insertPayment(payment: Payment, orderId: string): Promise<Payment>;
    generateId(prefix: string): string;
}
